export enum ContractStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  ENDED = 'ended',
  CANCELED = 'canceled',
  DISPUTED = 'disputed',
}

export enum GroupType {
  PROJECT = 'project',
  SERVICE = 'service',
  TEAM = 'team',
  JOB = 'job',
}
