export enum ListingStatus {
  Draft = 'draft',
  Published = 'published',
  Flagged = 'flagged',
  Archived = 'archived',
}

export enum ListingType {
  Project = 'project',
  Service = 'service',
  Job = 'job',
  Team = 'team',
}
